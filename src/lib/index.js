/**
 * Library Module
 * 
 * Pure utility functions for formatting, validation, and error handling.
 * These functions have no side effects and don't depend on application state.
 * 
 * @example
 * import { formatBytes, TransferError, validateFileMetadata } from '@/lib';
 */

export * from './formatters.js';
export * from './errors.js';
export * from './validators.js';
